# 刷刷英文:交接文件(給接手的 Claude)

寫於 2026-09-22。這份文件放在 repo 裡,跟著 git 走;記憶檔與制度正本不在 repo,要另外複製(見最後一節)。

## 0. 開場先做三件事
1. 讀 `C:\Users\bonni\.agents\institution\01_core.md`、`02_dispatch.md`,動工前讀 `07_user_profile.md`(她的制度正本;`~/.claude/CLAUDE.md` 只是路由)。
2. 讀 `~/.claude/projects/C--/memory/project_toeic_training.md`(這個專案的完整記憶,含所有教訓)。
3. 讀本文件的第 3、4 節,知道現在停在哪。

## 1. 這是什麼
- 站名「刷刷英文」,英文家教邱詩蘋(多益 955,數位媒體設計碩士)做給學生的免費英文練習站。純前端(HTML/CSS/原生 JS),GitHub Pages 託管,Supabase 存帳號與進度。
- 正式網址 https://www.shuashualanguage.com/ ;repo github.com/bonnie8536/toeic-training;本機 `C:\Users\bonni\Desktop\claude\toeic-training`。
- 內容量:P5 300 題、P6 45 組、P7 55 組、聽力 P1 25/P2 100/P3 20 組/P4 20 組、文章 40 篇(各配寫實配圖+同字新情境挖空考題)、文法 8 章 94 課 752 題、跟讀 218 句、聽寫 140、相似音 40、數字 40、寫作 55 單元、片語 120、檢測卷 28 題、模擬考、每日複習、學習記錄。

## 2. 工作方式(她驗證過、也會抽查的)
- 改題目一律改 `data/raw/*.json`,再跑 `python tools/merge_data.py` 產 `data/*.js`。**勿手改 data/*.js**。merge 有硬錯誤就不能上線。
- 新音檔:`python tools/gen_audio.py`(edge-tts+ffmpeg;已存在的檔會跳過,改了選項文字要先刪對應 mp3)。多段串接會自動重編碼成單一串流(避免手機重播只播一半)。
- 本機預覽:`.claude/launch.json` 的 `site`(python http.server 8754)。瀏覽器快取很黏,改 css/js 後用 `fetch(url,{cache:'reload'})` 再 reload。
- 內容生成走「產審分離」:Workflow 腳本(命題 agent + 獨立盲解/歧義攻擊/規範審查 agent + 失敗重寫),腳本都在 `tools/workflows/`。工作流常撞 session 額度,`resumeFromRunId` 續跑會回放已完成的 agent;結果在 tasks/<id>.output 的 `result` 欄。平行分組生成會跨組重複,合併前要本機 normalize 去重。
- 上線流程:本機預覽實測 → merge 無硬錯誤 → node --check js/*.js → commit(訊息中文,結尾 `Co-Authored-By: Claude ... <noreply@anthropic.com>`)→ push。她要的是「完成宣告＋可查證據」,不要熱情語氣。
- 她自己做所有登入(GitHub、Supabase、Cloudflare、ChatGPT);要用她的 Chrome 操作前先問。**永遠不要刪 repo**、不要 force push。

## 3. 現在的狀態(2026-09-22)
最近一批 commit(新→舊):`2737e02` 視覺系統第一步 → `4fc84f8` 單字首頁重整 → `ae883d9` 跟讀 218 句 → `0017c3a` 文法左側路徑 → `b145669` wish/hope 課修正 → `159e17d` 去 AI 味 60 項 → `c2cddef` 單字考題 40/40 → `364f9e4` 導覽群組+拖曳+公開註冊程式面。
- **視覺系統(她 09-20 拍板)**:方向「螢光筆+筆跡」。已上線:圓體標題與按鈕(Chiron GoRound TC,Google Fonts)、平的 12px 圓角按鈕、螢光筆(圓順色塊)標頁首標題與文法「目前這課」、上過的課用潦草筆跡圈起來、單字四玩法大圖示。**紅線:絕對不要厚底立體按鈕(box-shadow 0 Npx 0 那套)、不要幾何拼出來的角色、不換米白底。** 規則寫在 `css/style.css` 末段「視覺系統」註解(一頁螢光筆最多兩處)。
- **公開註冊**:程式面完成(登入視窗三分頁、驗證信、reset.html、terms.html、自助刪帳號)。**Supabase 後台她還沒做**,清單在 `公開註冊上線清單.md`(開 sign-up、開 Confirm email、Site URL/Redirect URLs、自訂 SMTP、跑 `tools/supabase_public_signup.sql`、自測)。沒接 SMTP 前不要對外宣布開放註冊。
- **維護**:`維護計畫.md`(每日存活/Supabase 心跳/merge 驗證可排程;每週流量/GSC/備份/新題)。排程代理要她點頭才建,她還沒回。

## 4. 待辦(照她提過的順序)
1. 視覺系統後續,等她回覆:答題回饋要不要改「老師紅筆批改」;題庫/模擬考/檢測要不要用「答案卡」語言;首頁主視覺、「刷刷」字標、角色、三種刷痕她要親手畫,拿到 SVG/PNG 後接進 `.page-head h1::before`、`.gx-node`、`.vg-icon`、首頁。
2. 單字訓練稽核尚未做的:字池不足時按鈕 disabled 並寫原因、來源改 chip、「只練漏接的字」模式。
3. 公開註冊:她做完後台後,用一個新 Gmail 走完註冊→驗證→登入→換裝置同步→忘記密碼→刪帳號;教師後台看得到新學生暱稱。
4. 每日維護排程(她點頭後建)。
5. 內容補充節奏:每週一小批(時事文章+P5 或聽力),走既有工作流。跟讀句庫剛擴到 218,聽寫 140 若她說快用完就照 `tools/workflows/gen-shadow-b3.js` 的模式再開。

## 5. 她的文案與版面規則(全站已依此清過一輪,新東西照做)
- 標題下不掛解釋句;副標只留規則與數字(題數、時間、老師看得到)。
- 不用冒號小標前綴(句意:/小提醒:/本輪成績:),直接寫內容或「答對 N / M 題」。
- 不用英文大寫小標(LEVEL/HINT)、不用裝飾編號(報告 1-4)、不用 emoji、不用破折號、不用「不是…而是…」。
- 結算頁全對不渲染 band-note;不寫情緒收尾(太好了/明天見)與功能預告(往下看解析);同一條規則全站只講一次。
- 功能性規則要留(可往回改答案、取消勾選不會刪掉)。
- 中文標點全形;英文半形。寫作三級保留她的命名「句子力/Email 力/論述力」。
- 全站英文詞彙限多益範圍(NGSL 3000+商業高頻);科普類禁捏造數據/研究/機構名。

## 6. 跨機生圖管線(她的 ChatGPT,帳號許怡節 Plus)
她的 Chrome 在另一台電腦(遠端桌面 DESKTOP-2M67A7S),下載/剪貼簿/localhost 都不通。流程:一則訊息只生一張圖(多圖會變成編輯上一張)→ 用座標點可見輸入框再 type+Return(點 find 的 ref 會點到隱藏的另一個 textbox)→ 完成判定=最後一張 estuary img 的 file id 變了且無「正在建立圖像」→ 點 分享 建共享連結(share id 用頁內 token 查 GET /backend-api/shared_conversations)→ 本機抓 share HTML 做 prompt↔file_id 配對(sediment:// 在其訊息之前=其回應,訊息新到舊)→ 本機 Browser pane 開 chatgpt.com,fetch `backend-anon/files/download/<fid>?shared_conversation_id=<sid>` 拿 download_url → canvas 轉 1200w jpg → window 變數超長回傳自動落檔 → `tools/workflows/decode_regen.py` 解碼 → **DELETE /backend-api/share/<id> 刪共享**(她的條件)。如果接手的電腦就是那台遠端機,Chrome 在本機,下載直接能用,整條管線可以簡化。

## 7. 常見坑
- Bash 工具指令超過約 250 行 heredoc 會被靜默截斷:大段程式先 Write 到 scratchpad 再 python 套用。
- Windows 主控台印中文會變亂碼:python 前加 `PYTHONIOENCODING=utf-8`。
- 桌機 `.topnav` 不可加 overflow-x:auto(會裁掉下拉選單,09-18 出過事)。
- 舊 Audio 不可設 src=''(會觸發 error 事件把播放鍵鎖死)。
- 大陸用語(網絡→網路)merge 會警告;工作流產出要掃一次。
- 學生登入前 `data-require-profile` 頁面會被登入視窗蓋住,本機測試用 JS 移掉 .modal-mask 即可。

## 8. 誠實聲明
- 公開註冊流程只在程式層驗過,未跑過真實寄信(SMTP 未接)。
- 手機 Safari 的音檔重播修復是依機制推論的(桌機 Chrome 兩版都能播完),她那邊要實測。
- 教師後台的上課紀錄寫入雲端未經她的教師帳號實測。
- 視覺系統只做了第一步;手畫線目前是 SVG 濾鏡模擬,不是真筆觸。

## 9. 搬到另一台電腦要複製的東西(repo 以外)
| 內容 | 這台的位置 | 新電腦放到 |
|---|---|---|
| 制度正本 | `C:\Users\bonni\.agents\institution\` | 同路徑(使用者名不同就改成該機的家目錄) |
| Claude 路由檔 | `C:\Users\bonni\.claude\CLAUDE.md` | `~/.claude/CLAUDE.md` |
| 自訂 agent | `C:\Users\bonni\.claude\agents\professor.md` | `~/.claude/agents/` |
| 專案記憶(所有專案) | `C:\Users\bonni\.claude\projects\C--\memory\` | `~/.claude/projects/C--/memory/`(在 `C:\` 開 Claude 才會對到 `C--`) |
| 這段對話(選配) | `C:\Users\bonni\.claude\projects\C--\27c708fc-37e1-4274-ad2f-027fb7f1c0da.jsonl` | 同路徑,然後 `claude --resume 27c708fc-37e1-4274-ad2f-027fb7f1c0da` |
新電腦要裝:Git、Python 3(+ `pip install edge-tts`)、ffmpeg、Node.js、Claude Code(桌面版或 CLI)、Chrome 擴充 Claude in Chrome(若要用她的 Chrome)。GitHub 推送權限她自己登入(`gh auth login` 或 Git Credential Manager)。
