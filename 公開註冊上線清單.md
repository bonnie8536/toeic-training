# 公開開放註冊:上線清單

網站端的程式已經做好(2026-09-18):登入視窗有「登入/註冊/忘記密碼」三個分頁、註冊填暱稱、驗證信流程、reset.html 重設密碼頁、terms.html 條款頁、會員選單可自助刪除帳號。
剩下的都在 Supabase 後台,需要妳登入操作(或授權我用妳的 Chrome 操作)。照順序做:

## 1. 資料庫:刪除帳號函式(必做)
SQL Editor → New query → 貼上 `tools/supabase_public_signup.sql` 整份 → Run。
沒做的話「刪除帳號」會失敗並提示改寫信給站長。

## 2. Authentication → URL Configuration(必做)
- Site URL:`https://www.shuashualanguage.com`
- Redirect URLs 加兩筆:`https://www.shuashualanguage.com/**` 與 `https://www.shuashualanguage.com/reset.html`
沒設的話,驗證信與重設信的連結會被導到 localhost。

## 3. Authentication → Sign In / Up → Email(必做)
- Allow new users to sign up:**開**(之前為了老師發帳號是關的)
- Confirm email:**開**(公開註冊一定要驗證,擋垃圾帳號)
- Minimum password length:8

## 4. 寄信服務(必做,不然驗證信寄不出去)
Supabase 內建的寄信只給測試用:每小時只能寄幾封、常進垃圾郵件、官方不保證送達。
Authentication → SMTP Settings → Enable Custom SMTP,填任一家免費方案:
- Resend(每月 3,000 封免費):註冊 resend.com → 驗證網域 shuashualanguage.com(到 Cloudflare 加它給的 DNS 記錄)→ 建 API key → SMTP host `smtp.resend.com`、port 465、user `resend`、password = API key、Sender email `no-reply@shuashualanguage.com`
- 或 Brevo(每日 300 封免費),設法相同。
設完用「忘記密碼」寄一封給自己測試。

## 5. Email Templates(建議)
Authentication → Email Templates,把 Confirm signup / Reset password 兩封改成中文,例如:
- 主旨:「刷刷英文:請完成註冊」/ 內文:「點下面的連結完成註冊:{{ .ConfirmationURL }}。如果不是你申請的,忽略這封信即可。」
- 主旨:「刷刷英文:重設密碼」/ 內文:「點下面的連結設定新密碼:{{ .ConfirmationURL }}」

## 6. 防機器人(建議,免費)
Authentication → Attack Protection → Enable Captcha → Cloudflare Turnstile。
到 Cloudflare 儀表板 → Turnstile → 新增 site(網域 shuashualanguage.com)拿 site key / secret key;secret 填 Supabase,site key 告訴我,我再把驗證元件加進登入視窗(沒開就不用加)。
Rate limits 那頁維持預設即可。

## 7. 上線前自測(全部用一個新的 Gmail)
1. 首頁右上角 → 登入 → 註冊分頁 → 填暱稱/Email/密碼 → 收到驗證信 → 點連結 → 回站已登入、右上角顯示暱稱。
2. 做幾題 → 換一台裝置登入 → 紀錄有同步。
3. 登出 → 忘記密碼 → 收信 → reset.html 設新密碼 → 能登入。
4. 教師後台看得到這個新學生(名稱=暱稱)。
5. 會員選單 → 刪除帳號 → 再登入應失敗。

## 8. 之後
- 免費版專案一週沒流量會休眠:有人固定使用就不會;休眠了到後台按 Restore。
- Supabase 免費版上限:50,000 MAU、500 MB 資料庫,對這個站綽綽有餘。
- 條款頁(terms.html)寫了聯絡信箱 bonnie8536@gmail.com,要換就改那一頁。
