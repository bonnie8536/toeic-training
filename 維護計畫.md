# 刷刷英文:更新與維護計畫

分成「機器可以自己做」和「要妳點頭才做」兩類。自動化的部分我可以設成排程代理(要妳同意後才建立),每次只在有問題或有產出時回報。

## 每日(自動,建議設排程,每天早上 8:00)
1. 網站存活:抓首頁、一個音檔、一張圖、data/articles.js,任一不是 200 就回報。
2. Supabase 心跳:對資料庫打一個匿名查詢(RLS 會回空,但算活動量),避免免費版一週沒流量就休眠,學生登入失敗。
3. 資料完整性:`python tools/merge_data.py` 跑一次,有硬錯誤就回報(防止有人手改到 data/)。
4. 只在異常時通知;一切正常就不吵妳。

## 每週(半自動,週一)
1. 流量:Cloudflare Web Analytics 近 7 天不重複訪客、最多人看的頁、來源;跟上週比,寫三行摘要。
2. Google Search Console:新索引/未索引頁、有沒有錯誤,sitemap 是否還在提交狀態。
3. 學生狀況:教師後台「本週動態」有幾個活躍學生、錯誤率最高的題目前 10(用 hist 紀錄算),題目答錯率 > 80% 的送我複查是不是題目本身有問題。
4. 備份:把 Supabase progress 表匯出成 JSON 存到 Desktop\claude\toeic-training-backup\(要妳的教師帳號登入一次,或給我 service key 走排程)。
5. 內容:每週加一小批新題(例如 1 篇時事文章 + 10 題 Part 5,或 5 題聽力),走既有的命題→獨立驗證→merge 流程。主題妳指定,沒指定就照「時事/科普/職場」輪。

## 每月(要妳決定)
1. 題目品質複查:把當月錯誤率異常(太高或太低)的題目列出,判斷是題目問題還是教學重點,該修的修、該刪的刪。
2. 相依檢查:supabase-js CDN 版本、edge-tts 聲線是否還在、ffmpeg 可用。
3. 條款與設定:terms.html 內容是否還正確(聯絡信箱、服務商)、Supabase 免費額度用量(MAU、資料庫大小、寄信量)。
4. 品牌與版面:對照「去 AI 味」清單再掃一次新加的頁面。

## 隨時(事件驅動)
- GSC 寄「未索引」信 → 我處理(之前處理過一次,多半是 noindex 的內頁,正常)。
- Supabase 寄休眠或額度通知 → 妳轉給我。
- 學生回報 bug → 妳貼給我,我在本機預覽重現後修。
- 上線新功能前一律:本機預覽實測 → merge 驗證 → commit → push;不直接改 data/*.js。

## 現在可以馬上做的
- 每日排程代理:妳說一聲我就建(用 Claude Code 的排程功能,跑上面「每日」三項,異常才通知)。
- 每週摘要:同樣可以排程,週一早上產一份三行摘要放到 Desktop\claude\toeic-training\週報\。

## 需要妳提供
- Cloudflare 與 GSC 目前都是妳的帳號登入才看得到:排程要跑就得授權我用妳的 Chrome,或妳每週自己截圖給我。
- Supabase 備份:service key(只存本機、不進 git)或每週妳登入一次。
